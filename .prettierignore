# Package manager
node_modules/
package-lock.json

# Build output
build/
.svelte-kit/
.vite/

# Supabase (SQL has its own formatting conventions)
supabase/

# Generated by `supabase gen types typescript --local` (npm run gen:types).
# Prettier-on-save reformats semicolons, diverging from generator output and
# breaking migration-smoke's `git diff --exit-code src/lib/types/database.ts` step.
src/lib/types/database.ts
